manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
  projects:
    - name: zmk
      remote: zmkfirmware
      # Pinned rather than tracking main: this session confirmed the
      # config/boards/shields/<name> layout we use (GYE-269+) prints a
      # deprecation warning on current main ("use a module instead") and
      # could be removed outright in a future west update. Pin to the
      # exact commit validated to build, and bump deliberately.
      revision: 904c9aec8822d79149d42c8a9a77e8828eb08f5a
      import: app/west.yml
  self:
    path: config
